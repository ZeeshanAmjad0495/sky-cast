interface StationData {
  contribution: number;
  distance: number;
  id: string;
  latitude: number;
  longitude: number;
  name: string;
  quality: number;
  useCount: number;
}

export default StationData;
